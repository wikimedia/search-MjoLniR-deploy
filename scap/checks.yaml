checks:
    virtualenv_cirrus:
        type: script
        stage: fetch
        timeout: 300
        group: cirrus
        command: virtualenv.sh
    virtualenv_relforge:
        type: script
        stage: fetch
        timeout: 300
        group: relforge
        command: virtualenv.sh
    virtualenv_analytics:
        type: script
        stage: fetch
        timeout: 300
        group: analytics
        command: virtualenv.sh
    spark_config:
        type: command
        stage: fetch
        timeout: 300
        group: analytics
        command: spark.sh

